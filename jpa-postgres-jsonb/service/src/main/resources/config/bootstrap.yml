spring:
  application:
    name: book
  cloud:
    config:
#      uri: ${vcap.services.${PREFIX:}configserver.credentials.uri:http://user:password@localhost:8080}
      enabled: false
    discovery:
      enabled: true